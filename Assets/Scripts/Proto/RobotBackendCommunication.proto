syntax = "proto3";

package robotbackendcommunication;

message Screenshot {
  bytes image = 1;
}

// int action for agent
message AgentAction {
  int32 action = 1;
}

service RobotBackendCommunicator {
  rpc GetAction(Screenshot) returns (AgentAction) {}
}
